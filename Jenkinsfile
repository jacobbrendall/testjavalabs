pipeline {
    agent any

    stages {
        stage ('Initialize') {
            steps {
                sh
                    echo 'This is Initialize'

            }
        }

        stage ('Build') {
            steps {
                echo 'This is a minimal pipeline.'
            }
        }
    }
}